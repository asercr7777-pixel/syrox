# Marketplace artwork assets

This directory is reserved for the marketplace's visual assets. The existing Frames and Backgrounds are intentionally left unchanged.

The marketplace currently renders item artwork through React/SVG components. New artwork assets should be added here only when they are wired to the corresponding item IDs and verified with a production build.