/**
 * Marketplace artwork asset manifest.
 *
 * The visual system intentionally uses deterministic SVG artwork so every
 * marketplace item has a stable, lightweight asset without shipping a large
 * bitmap bundle. Frames and backgrounds are not included here.
 */
export const MARKETPLACE_ARTWORK_VERSION = '1';
