import { useEffect, useMemo, useState, type ReactNode } from 'react';
import { Activity, ArrowRight, Brain, Check, Dumbbell, Flame, Gauge, RotateCcw, Target, Timer, UserRound, ChevronLeft, ChevronRight } from 'lucide-react';

type Profile={weight:number;height:number;age:number;level:string;goal:string;days:number;duration:number;equipment:string;focus:string};
type Exercise={name:string;sets:number;reps:string;rest:string;note:string;level:number};
const defaults:Profile={weight:70,height:175,age:18,level:'intermediate',goal:'strength',days:4,duration:50,equipment:'none',focus:'full body'};
const library:Record<string,Exercise[]>={strength:[{name:'Tempo Push-Ups',sets:4,reps:'8–15',rest:'60–90s',note:'3 sec down, explosive up.',level:2},{name:'Pike Push-Ups',sets:4,reps:'6–12',rest:'75s',note:'Keep hips high and control the descent.',level:2},{name:'Split Squats',sets:4,reps:'10–14 / leg',rest:'75s',note:'Drive through the front foot.',level:1},{name:'Single-Leg Glute Bridge',sets:3,reps:'12–18 / leg',rest:'45s',note:'Pause hard at the top.',level:1},{name:'Hollow Body Hold',sets:3,reps:'25–45 sec',rest:'45s',note:'Lower back stays pressed down.',level:1},{name:'Explosive Push-Ups',sets:3,reps:'5–8',rest:'90s',note:'Quality reps only; land softly.',level:3}],muscle:[{name:'Push-Up Mechanical Drop Set',sets:3,reps:'8–15 + 6–10',rest:'90s',note:'Standard → knees without resting.',level:2},{name:'Pike Push-Ups',sets:4,reps:'8–12',rest:'75s',note:'Shoulders lead the movement.',level:2},{name:'Reverse Lunges',sets:4,reps:'10–15 / leg',rest:'60s',note:'Controlled range, stable knee.',level:1},{name:'Squat 1.5 Reps',sets:4,reps:'10–15',rest:'60s',note:'Halfway up, return down, then stand.',level:2},{name:'Plank Shoulder Taps',sets:3,reps:'20–30 total',rest:'45s',note:'Minimize hip rotation.',level:1}],athletic:[{name:'Pogo Jumps',sets:4,reps:'20',rest:'45s',note:'Quick contacts; stay springy.',level:1},{name:'Broad Jumps',sets:5,reps:'4',rest:'90s',note:'Full reset between jumps.',level:2},{name:'Explosive Push-Ups',sets:4,reps:'5–8',rest:'90s',note:'Maximum quality, never grind.',level:3},{name:'Jump Squats',sets:4,reps:'6–10',rest:'75s',note:'Soft landing and full reset.',level:2},{name:'Reverse Lunges',sets:3,reps:'10 / leg',rest:'60s',note:'Stay tall and controlled.',level:1},{name:'Hollow Body Hold',sets:3,reps:'30–45 sec',rest:'45s',note:'Brace throughout.',level:1}]};
function bmi(p:Profile){const h=p.height/100;return p.weight/(h*h)}
function levelScore(p:Profile){return Math.round(Math.min(100,25+(p.level==='beginner'?10:p.level==='advanced'?35:25)+(p.days*4)+(p.duration>=60?15:8)))}
function buildPlan(p:Profile,week:number,day:number):Exercise[]{const key=p.focus==='explosiveness'||p.goal==='athletic'?'athletic':p.goal==='muscle'?'muscle':'strength';const max=p.level==='beginner'?2:3;const base=library[key].filter(e=>e.level<=max);const rotated=[...base.slice(day%base.length),...base.slice(0,day%base.length)];const count=p.duration<40?4:6;return rotated.slice(0,count).map(e=>({...e,sets:Math.max(2,Math.min(5,e.sets+(week>=3&&e.level<3?1:0))),note:week===4?`${e.note} Deload: stop with 2–3 reps in reserve.`:week===2?`${e.note} Add 1–2 quality reps when form is perfect.`:week===3?`${e.note} Progress slowly; keep every rep clean.`:e.note}))}

export default function ShadowAICinematic(){
 const [profile,setProfile]=useState<Profile>(defaults);const [onboarded,setOnboarded]=useState(false);const [tab,setTab]=useState<'plan'|'profile'>('plan');const [completed,setCompleted]=useState<number[]>([]);const [week,setWeek]=useState(1);const [day,setDay]=useState(1);
 useEffect(()=>{try{const raw=localStorage.getItem('syrox-shadow-coach');if(raw){const s=JSON.parse(raw);setProfile(s.profile??defaults);setOnboarded(Boolean(s.onboarded));setWeek(s.week??1);setDay(s.day??1);setCompleted(s.completed??[])}}catch{}}
 ,[]);useEffect(()=>{if(onboarded)try{localStorage.setItem('syrox-shadow-coach',JSON.stringify({profile,onboarded,week,day,completed}))}catch{}},[profile,onboarded,week,day,completed]);
 const plan=useMemo(()=>buildPlan(profile,week,day),[profile,week,day]);const score=levelScore(profile);const finish=()=>{setCompleted([]);setDay(d=>d>=profile.days?1:d+1);if(day>=profile.days)setWeek(w=>Math.min(4,w+1))};
 if(!onboarded)return <div className="min-h-[70vh] rounded-3xl border border-white/10 bg-black/40 p-5 text-white shadow-2xl backdrop-blur-xl"><div className="mx-auto max-w-3xl"><div className="mb-8 flex items-center gap-3"><div className="rounded-2xl bg-white/10 p-3"><Brain/></div><div><div className="text-xs font-bold uppercase tracking-[.28em] text-white/50">SHADOW // COACH</div><h1 className="text-3xl font-black sm:text-5xl">Build your body.<br/><span className="text-white/50">No generic plans.</span></h1></div></div><p className="mb-7 max-w-2xl text-sm leading-6 text-white/60">Shadow assesses your training profile and builds a four-week progression around your goal, time and equipment.</p><div className="grid gap-4 sm:grid-cols-2">{[['weight','Weight (kg)'],['height','Height (cm)'],['age','Age']].map(([k,l])=><label key={k} className="text-xs font-bold uppercase tracking-wider text-white/50">{l}<input type="number" value={(profile as any)[k]} onChange={e=>setProfile({...profile,[k]:Number(e.target.value)})} className="mt-2 w-full rounded-xl border border-white/10 bg-white/5 p-3 text-base text-white outline-none"/></label>)}<Select label="Training level" value={profile.level} onChange={v=>setProfile({...profile,level:v})} options={['beginner','intermediate','advanced']}/><Select label="Main goal" value={profile.goal} onChange={v=>setProfile({...profile,goal:v})} options={['strength','muscle','athletic']}/><Select label="Primary focus" value={profile.focus} onChange={v=>setProfile({...profile,focus:v})} options={['full body','explosiveness','upper body','lower body']}/><label className="text-xs font-bold uppercase tracking-wider text-white/50">Training days / week<input type="number" min="2" max="7" value={profile.days} onChange={e=>setProfile({...profile,days:Math.max(2,Math.min(7,Number(e.target.value)))})} className="mt-2 w-full rounded-xl border border-white/10 bg-white/5 p-3 text-white"/></label><Select label="Session length" value={String(profile.duration)} onChange={v=>setProfile({...profile,duration:Number(v)})} options={['30','45','50','60','75']} suffix=" min"/><Select label="Equipment" value={profile.equipment} onChange={v=>setProfile({...profile,equipment:v})} options={['none','home','gym']}/></div><button onClick={()=>{setOnboarded(true);setCompleted([]);setWeek(1);setDay(1)}} className="mt-7 flex w-full items-center justify-center gap-2 rounded-xl bg-white py-4 font-black text-black">GENERATE 4-WEEK PLAN <ArrowRight size={18}/></button></div></div>;
 const toggle=(i:number)=>setCompleted(v=>v.includes(i)?v.filter(x=>x!==i):[...v,i]);const allDone=completed.length===plan.length;
 return <div className="min-h-[70vh] text-white"><div className="mb-4 flex flex-wrap items-center justify-between gap-3"><div><div className="text-xs font-bold uppercase tracking-[.28em] text-white/40">SHADOW // PERSONAL COACH</div><h1 className="text-3xl font-black sm:text-4xl">Your Training System</h1></div><button onClick={()=>setOnboarded(false)} className="flex items-center gap-2 rounded-xl border border-white/10 bg-white/5 px-3 py-2 text-xs font-bold"><RotateCcw size={14}/> Reassess</button></div><div className="mb-4 grid gap-3 sm:grid-cols-4"><Stat icon={<Gauge/>} label="Coach score" value={`${score}/100`}/><Stat icon={<Target/>} label="Goal" value={profile.goal}/><Stat icon={<Flame/>} label="Week" value={`${week} / 4`}/><Stat icon={<Activity/>} label="BMI" value={bmi(profile).toFixed(1)}/></div><div className="mb-4 flex flex-wrap items-center gap-2"><button onClick={()=>setTab('plan')} className={`rounded-xl px-4 py-2 text-sm font-bold ${tab==='plan'?'bg-white text-black':'bg-white/5 text-white/60'}`}>Today's Plan</button><button onClick={()=>setTab('profile')} className={`rounded-xl px-4 py-2 text-sm font-bold ${tab==='profile'?'bg-white text-black':'bg-white/5 text-white/60'}`}>Athlete Profile</button><div className="ml-auto flex items-center gap-1 rounded-xl border border-white/10 bg-white/5 p-1"><button onClick={()=>setDay(d=>Math.max(1,d-1))} className="rounded-lg p-2"><ChevronLeft size={15}/></button><span className="px-2 text-xs font-black">DAY {String(day).padStart(2,'0')}</span><button onClick={()=>setDay(d=>Math.min(profile.days,d+1))} className="rounded-lg p-2"><ChevronRight size={15}/></button></div></div>{tab==='profile'?<div className="rounded-2xl border border-white/10 bg-black/30 p-5"><div className="mb-5 flex items-center gap-3"><UserRound/><div><b className="block">Assessment profile</b><span className="text-xs text-white/50">Saved locally so your progression survives reloads.</span></div></div><div className="grid grid-cols-2 gap-3 sm:grid-cols-4">{[['Weight',`${profile.weight} kg`],['Height',`${profile.height} cm`],['Age',String(profile.age)],['Level',profile.level],['Days',`${profile.days}/week`],['Duration',`${profile.duration} min`],['Equipment',profile.equipment],['Goal',profile.goal]].map(([a,b])=><div key={a} className="rounded-xl bg-white/5 p-3"><span className="block text-[10px] uppercase tracking-wider text-white/40">{a}</span><b className="mt-1 block capitalize">{b}</b></div>)}</div></div>:<><div className="mb-4 rounded-2xl border border-white/10 bg-gradient-to-br from-white/10 to-transparent p-5"><div className="mb-1 flex items-center gap-2 text-xs font-bold uppercase tracking-widest text-white/50"><Dumbbell size={14}/> Week {week} · Day {day} · {profile.goal} protocol</div><h2 className="text-2xl font-black">Progressive {profile.goal} session</h2><p className="mt-1 text-sm text-white/50">{plan.length} movements · {profile.duration} min · {week===4?'Recovery / deload focus':'Build quality before intensity'}</p></div><div className="space-y-2">{plan.map((e,i)=><button key={`${e.name}-${i}`} onClick={()=>toggle(i)} className={`flex w-full items-center gap-3 rounded-2xl border p-4 text-left transition ${completed.includes(i)?'border-white/20 bg-white/10 opacity-60':'border-white/10 bg-black/30 hover:bg-white/5'}`}><span className={`flex h-9 w-9 shrink-0 items-center justify-center rounded-full ${completed.includes(i)?'bg-white text-black':'bg-white/5'}`}>{completed.includes(i)?<Check size={17}/>:<span className="text-xs font-black">{i+1}</span>}</span><span className="min-w-0 flex-1"><b className="block">{e.name}</b><span className="text-xs text-white/45">{e.sets} sets · {e.reps} · Rest {e.rest}</span><small className="mt-1 block text-xs text-white/35">{e.note}</small></span><Timer size={16} className="text-white/30"/></button>)}</div><div className="mt-4 rounded-2xl border border-white/10 bg-black/30 p-4"><div className="mb-2 flex justify-between text-xs font-bold"><span>Session progress</span><span>{completed.length}/{plan.length}</span></div><div className="h-2 overflow-hidden rounded-full bg-white/10"><div className="h-full bg-white transition-all" style={{width:`${completed.length/Math.max(1,plan.length)*100}%`}}/></div>{allDone&&<button onClick={finish} className="mt-4 w-full rounded-xl bg-white py-3 text-sm font-black text-black">COMPLETE DAY {day} <ArrowRight size={15} className="ml-1 inline"/></button>}</div></>}<div className="mt-5 flex items-center gap-2 text-[11px] leading-5 text-white/35"><Brain size={13}/> Training guidance is a planning aid. Pain, injury and recovery always override the generated plan.</div></div>
}
function Select({label,value,onChange,options,suffix='' }:{label:string;value:string;onChange:(v:string)=>void;options:string[];suffix?:string}){return <label className="text-xs font-bold uppercase tracking-wider text-white/50">{label}<select value={value} onChange={e=>onChange(e.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-white/5 p-3 text-white">{options.map(o=><option key={o} value={o}>{o}{suffix}</option>)}</select></label>}
function Stat({icon,label,value}:{icon:ReactNode;label:string;value:string}){return <div className="rounded-2xl border border-white/10 bg-black/30 p-4"><div className="mb-3 text-white/40">{icon}</div><span className="block text-[10px] uppercase tracking-widest text-white/35">{label}</span><b className="mt-1 block capitalize">{value}</b></div>}
