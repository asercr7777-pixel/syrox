import { useEffect,useState } from 'react';
import { Crown,RefreshCw,Users,Shield,Trophy,Flame,Target,Zap } from 'lucide-react';
import { useStore } from '../store/useStore';
import { useAuth } from '../lib/auth';
import { supabase,isSupabaseConfigured } from '../lib/supabase';
import { getRankByXp,getNextRank } from '../data/ranks';
import { UserAvatar } from '../components/ui/UserAvatar';
import { toast } from '../components/ui/Toast';
type Mode='players'|'clans';type Sort='xp'|'discipline_score'|'streak'|'total_points';type Player={user_id:string;username:string;avatar:string;xp:number;level:number;total_points:number;streak:number;discipline_score:number;tasks_completed:number;rank_name:string};type Clan={season_id:string;season_number:number;season_name:string;clan_id:string;clan_name:string;emblem:string;image_url:string|null;level:number;xp:number;rank:number;member_count:number};
const sorts:{key:Sort;label:string;icon:any}[]=[{key:'xp',label:'XP',icon:Trophy},{key:'discipline_score',label:'Discipline',icon:Target},{key:'streak',label:'Streak',icon:Flame},{key:'total_points',label:'Score',icon:Zap}];
export function LeaderboardRebuilt(){const {state,syncLeaderboard}=useStore();const {user}=useAuth();const [mode,setMode]=useState<Mode>('players');const [sort,setSort]=useState<Sort>('xp');const [players,setPlayers]=useState<Player[]>([]);const [clans,setClans]=useState<Clan[]>([]);const [loading,setLoading]=useState(true);const current=getRankByXp(state.xp),next=getNextRank(state.xp);
 const load=async()=>{if(!isSupabaseConfigured()){setLoading(false);return}setLoading(true);try{await syncLeaderboard();const [{data:p,error:pe},{data:c,error:ce}]=await Promise.all([supabase.from('leaderboard').select('*').order(sort,{ascending:false}).limit(100),supabase.from('clan_season_leaderboard').select('*').order('rank',{ascending:true}).limit(100)]);if(pe)throw pe;if(ce)throw ce;setPlayers((p??[]) as Player[]);setClans((c??[]) as Clan[])}catch(e){console.error(e);toast({title:'Could not synchronize rankings',type:'error'})}finally{setLoading(false)}};useEffect(()=>{void load()},[user?.id,sort]);const position=players.findIndex(p=>p.user_id===user?.id)+1;const progress=next?Math.round(Math.max(0,Math.min(100,(state.xp-current.xpRequired)/(next.xpRequired-current.xpRequired)*100))):100;
 return <div className="space-y-6 pb-8"><header className="relative overflow-hidden rounded-[1.75rem] border border-white/10 bg-ink-950/70 p-5 sm:p-8"><div className="absolute -right-24 -top-28 h-72 w-72 rounded-full bg-gold-500/10 blur-3xl"/><div className="relative grid gap-7 lg:grid-cols-[1.1fr_.9fr] lg:items-end"><div><div className="mb-3 flex items-center gap-2 text-[10px] font-bold uppercase tracking-[.3em] text-gold-400"><Crown size={14}/> Ranking Command</div><h1 className="font-display text-3xl font-black uppercase sm:text-5xl">Season Rankings</h1><p className="mt-3 max-w-xl text-sm leading-6 text-ink-300">One source of truth: live progression records for players and real seasonal clan contribution.</p></div><div className="rounded-2xl border border-white/10 bg-black/30 p-5"><div className="flex justify-between text-xs uppercase tracking-widest text-ink-500"><span>Current rank</span><span>{state.seasonXp.toLocaleString()} season XP</span></div><p className="mt-3 font-display text-2xl font-black" style={{color:current.color}}>{current.name}</p><div className="mt-4 h-2 rounded-full bg-ink-900"><div className="h-full rounded-full bg-gradient-to-r from-ember-500 to-gold-500" style={{width:`${progress}%`}}/></div></div></div></header>
 <div className="rounded-2xl border border-white/10 bg-black/25 p-1.5"><div className="grid grid-cols-2 gap-1"><button onClick={()=>setMode('players')} className={`flex items-center justify-center gap-2 rounded-xl px-4 py-3 text-xs font-black uppercase tracking-widest ${mode==='players'?'bg-ember-500/15 text-ember-400':'text-ink-500'}`}><Users size={15}/> Players</button><button onClick={()=>setMode('clans')} className={`flex items-center justify-center gap-2 rounded-xl px-4 py-3 text-xs font-black uppercase tracking-widest ${mode==='clans'?'bg-ember-500/15 text-ember-400':'text-ink-500'}`}><Shield size={15}/> Clans</button></div></div>
 {mode==='players'?<section className="rounded-[1.5rem] border border-white/10 bg-black/25 p-4 sm:p-5"><div className="flex flex-col gap-4 lg:flex-row lg:items-center lg:justify-between"><div><p className="text-[10px] font-bold uppercase tracking-[.25em] text-ember-400">Live standings</p><h2 className="mt-1 font-display text-xl font-bold uppercase">Player Ladder</h2><p className="mt-1 text-xs text-ink-500">Real stored progression · top 100</p></div><div className="flex flex-wrap gap-1.5">{sorts.map(({key,label,icon:Icon})=><button key={key} onClick={()=>setSort(key)} className={`btn-ghost text-xs ${sort===key?'bg-ember-500/15 text-ember-400':''}`}><Icon size={13}/> {label}</button>)}<button onClick={()=>void load()} className="btn-ghost text-xs"><RefreshCw size={13}/> Refresh</button></div></div>{position>0&&<div className="mt-3 flex justify-between rounded-xl border border-ember-500/25 bg-ember-500/[.06] px-4 py-3 text-xs"><span>Your current position</span><b className="font-mono text-ember-400">#{position}</b></div>}<div className="mt-4 overflow-hidden rounded-xl border border-white/5">{loading?<Loading/>:players.length===0?<Empty text="No real player data available yet."/>:players.map((p,i)=><div key={p.user_id} className={`flex min-h-[68px] items-center gap-3 border-b border-white/5 px-3 py-3 last:border-0 sm:px-4 ${p.user_id===user?.id?'bg-ember-500/[.08]':''}`}><span className="w-7 text-center font-mono text-xs text-ink-600">{i+1}</span><UserAvatar avatar={p.avatar} rank={getRankByXp(Number(p.xp)||0)} size="sm"/><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold">{p.username||'Hunter'} {p.user_id===user?.id&&<span className="text-[10px] text-ember-400">YOU</span>}</p><p className="truncate text-[10px] text-ink-500">{p.rank_name||getRankByXp(Number(p.xp)||0).name} · {Number(p.streak)||0} day streak · {Number(p.discipline_score)||0}% discipline</p></div><div className="text-right"><p className="font-mono text-sm font-bold">{Number(p.xp||0).toLocaleString()}</p><p className="text-[9px] uppercase text-ink-600">XP</p></div></div>)}</div></section>:<section className="rounded-[1.5rem] border border-white/10 bg-black/25 p-4 sm:p-5"><div><p className="text-[10px] font-bold uppercase tracking-[.25em] text-ember-400">Real clan standings</p><h2 className="mt-1 font-display text-xl font-bold uppercase">Clan Ladder</h2><p className="mt-1 text-xs text-ink-500">Season XP is the sum of real member contributions.</p></div><div className="mt-5 space-y-2">{loading?<Loading/>:clans.length===0?<Empty text="No clans have seasonal contribution yet."/>:clans.map(c=><div key={c.clan_id} className="flex min-h-[76px] items-center gap-3 rounded-xl border border-white/5 bg-white/[.02] px-4 py-3"><span className="w-7 font-mono text-xs text-ink-600">#{c.rank}</span><div className="flex h-11 w-11 shrink-0 items-center justify-center overflow-hidden rounded-xl border border-white/10 bg-black/40 text-lg">{c.image_url?<img src={c.image_url} alt="" className="h-full w-full object-cover"/>:c.emblem||'⚔️'}</div><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold">{c.clan_name}</p><p className="text-[10px] text-ink-500">Level {c.level} · {c.member_count} members · Season {c.season_number}</p></div><div className="text-right"><p className="font-mono text-sm font-bold">{Number(c.xp||0).toLocaleString()}</p><p className="text-[9px] uppercase text-ink-600">XP</p></div></div>)}</div></section>}
 </div>}
function Loading(){return <div className="py-12 text-center"><RefreshCw size={24} className="mx-auto mb-2 animate-spin text-ink-500"/><p className="text-sm text-ink-500">Synchronizing...</p></div>}function Empty({text}:{text:string}){return <div className="py-12 text-center text-sm text-ink-500">{text}</div>}
