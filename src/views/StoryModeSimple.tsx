import { useMemo, useState } from 'react';
import { ArrowLeft, Check, ChevronRight, Coins, Crown, Lock, Map, Mountain, Skull, Sparkles, Swords, Target, Trophy, Volume2, VolumeX, Zap } from 'lucide-react';
import { useStore } from '../store/useStore';
import { ALL_CHAPTERS } from '../data/story';
import type { StoryChapter, StoryMission } from '../data/story/types';
import { isMusicEnabled, setMusicEnabled } from '../lib/audioEngine';
import { toast } from '../components/ui/Toast';
import { triggerConfetti } from '../components/ui/Confetti';

type Screen='world'|'chapter'|'boss'|'reward';
const chapters=[...ALL_CHAPTERS].sort((a,b)=>a.number-b.number).slice(0,30);
const nodes=[[7,78],[14,67],[22,73],[29,62],[36,70],[43,57],[50,65],[58,53],[66,61],[74,49],[82,56],[91,43],[84,30],[74,25],[63,34],[53,24],[43,31],[33,20],[23,30],[13,21],[19,48],[29,44],[39,50],[49,42],[59,45],[69,40],[79,38],[88,27],[61,76],[36,84]];
const regions=[['ASHEN GATE',0,5],['VEILWOOD',6,11],['IRON WASTES',12,17],['DUSK VALLEY',18,23],['CROWN OF SHADOWS',24,29]];
const pathFor=(i:number)=>i===0?'M 7 78 '+nodes.slice(1).map(([x,y])=>`L ${x} ${y}`).join(' '):'';
const dateKey=(t:number)=>new Date(t).toISOString().slice(0,10);
function progress(state:ReturnType<typeof useStore>['state'],m:StoryMission){switch(m.type){case'tasks':return Math.min(m.target,Object.values(state.coreCompleted).filter(Boolean).length+Object.values(state.customCompleted).filter(Boolean).length);case'workout':{const today=dateKey(Date.now());const sessions=state.workoutSessions.filter(s=>dateKey(s.completedAt)===today).length;const history=state.history.some(d=>d.date===today&&d.workoutCompleted);return Math.min(m.target,Math.max(state.workoutsCompletedToday,sessions,history?1:0))}case'pray':return state.coreCompleted.pray?1:0;case'water':return state.coreCompleted.water?1:0;case'sleep':return state.coreCompleted.sleep?1:0;case'read_quran':return state.coreCompleted.read_quran?1:0;case'read_book':return state.coreCompleted.read?1:0;case'streak':return Math.min(m.target,state.streak);case'dungeon':return Math.min(m.target,state.dungeonsCleared);case'discipline_score':{const enabled=state.mainTasks.filter(t=>t.enabled);const done=enabled.filter(t=>state.coreCompleted[t.id]).length;return enabled.length?Math.round(done/enabled.length*100):0}default:return 0}}
export default function StoryModeSimple(){const{state,completeStoryMission,defeatStoryBoss,advanceStoryChapter,unlockLore,unlockStoryAchievement}=useStore();const[screen,setScreen]=useState<Screen>('world');const[selected,setSelected]=useState<StoryChapter>(chapters[0]);const[bossPhase,setBossPhase]=useState(0);const[reward,setReward]=useState<{xp:number;coins:number;title?:string;lore?:string}|null>(null);const[music,setMusic]=useState(isMusicEnabled());const current=Math.min(state.storyChapter+1,30);const rows=useMemo(()=>selected.missions.map(m=>({m,p:progress(state,m),done:Boolean(state.storyCompletedMissions[m.id])})),[selected,state]);const done=rows.filter(x=>x.done).length;const open=(c:StoryChapter)=>{if(c.number>current)return;setSelected(c);setScreen('chapter')};const complete=(m:StoryMission)=>{if(state.storyCompletedMissions[m.id])return;const p=progress(state,m);if(p<m.target){toast({title:'Mission not complete',message:`Do the objective first: ${m.title}`,type:'info',icon:'◈'});return}completeStoryMission(m.id,{xp:m.xpReward,coins:m.coinReward});toast({title:'Mission complete',message:`+${m.xpReward} XP · +${m.coinReward} coins`,type:'success',icon:'✓'})};const hitBoss=()=>{if(bossPhase<2){setBossPhase(v=>v+1);return}const b=selected.boss;defeatStoryBoss(b.id);if(b.rewardTitle)unlockStoryAchievement(b.rewardTitle);if(b.rewardLore)unlockLore(b.rewardLore);completeStoryMission(`boss_${b.id}`,{xp:b.xpReward,coins:b.coinReward});setReward({xp:b.xpReward,coins:b.coinReward,title:b.rewardTitle,lore:b.rewardLore});triggerConfetti(30);setScreen('reward')};const claim=()=>{setReward(null);if(selected.number===current&&selected.number<30)advanceStoryChapter();setScreen('world')};
if(screen==='reward'&&reward)return <div className="relative min-h-[78vh] overflow-hidden rounded-[2rem] border border-white/10 bg-[#050607] p-6 text-white"><div className="absolute inset-0 bg-[radial-gradient(circle_at_center,rgba(255,255,255,.1),transparent_45%)]"/><div className="relative mx-auto flex min-h-[68vh] max-w-xl flex-col items-center justify-center text-center"><Trophy size={42}/><div className="mt-5 text-xs font-black uppercase tracking-[.35em] text-white/40">TERRITORY CONQUERED</div><h1 className="mt-3 text-4xl font-black">{selected.title}</h1><div className="my-7 flex gap-3"><span className="rounded-xl bg-white/10 px-4 py-3 font-black"><Zap size={15} className="mr-2 inline"/>+{reward.xp} XP</span><span className="rounded-xl bg-white/10 px-4 py-3 font-black"><Coins size={15} className="mr-2 inline"/>+{reward.coins}</span></div>{reward.title&&<div className="mb-6 rounded-xl border border-white/10 bg-white/5 px-5 py-3"><b>{reward.title}</b><span className="ml-2 text-xs text-white/40">Achievement unlocked</span></div>}<button onClick={claim} className="rounded-xl bg-white px-7 py-3 font-black text-black">Return to World <ChevronRight size={17} className="ml-1 inline"/></button></div></div>;
if(screen==='boss')return <div className="min-h-[78vh] rounded-[2rem] border border-white/10 bg-[#050607] p-5 text-white"><button onClick={()=>setScreen('chapter')} className="mb-6 flex items-center gap-2 text-xs font-bold text-white/45"><ArrowLeft size={15}/> World</button><div className="mx-auto max-w-2xl rounded-[2rem] border border-white/10 bg-black/50 p-7 text-center"><Skull className="mx-auto" size={42}/><div className="mt-4 text-xs font-black uppercase tracking-[.35em] text-white/35">BOSS CITADEL · PHASE {bossPhase+1}/3</div><h1 className="mt-3 text-4xl font-black">{selected.boss.name}</h1><p className="mt-2 text-sm text-white/40">{['Hold your ground.','Break the pattern.','Finish the fight.'][bossPhase]}</p><div className="my-7 h-2 rounded-full bg-white/10"><div className="h-full rounded-full bg-white transition-all" style={{width:`${100-bossPhase*33.33}%`}}/></div><button onClick={hitBoss} className="w-full rounded-xl bg-white py-4 font-black text-black">{bossPhase===2?'DEFEAT BOSS':'COMPLETE PHASE'} <ChevronRight size={17} className="ml-1 inline"/></button></div></div>;
if(screen==='chapter')return <div className="min-h-[78vh] text-white"><div className="mb-4 flex items-center justify-between"><button onClick={()=>setScreen('world')} className="flex items-center gap-2 text-xs font-bold text-white/45"><ArrowLeft size={15}/> World Map</button><button onClick={()=>{const n=!music;setMusic(n);setMusicEnabled(n)}} className="rounded-xl border border-white/10 bg-white/5 p-2">{music?<Volume2 size={16}/>:<VolumeX size={16}/>}</button></div><div className="mb-4 rounded-[2rem] border border-white/10 bg-black/40 p-6"><div className="text-xs font-black uppercase tracking-[.3em] text-white/35">{selected.region.name} · CHAPTER {String(selected.number).padStart(2,'0')}</div><div className="mt-2 flex items-end justify-between gap-4"><div><h1 className="text-3xl font-black sm:text-4xl">{selected.title}</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-white/45">{selected.description}</p></div><div className="text-2xl font-black">{Math.round(done/Math.max(1,rows.length)*100)}%</div></div><div className="mt-5 h-2 rounded-full bg-white/10"><div className="h-full rounded-full bg-white" style={{width:`${done/Math.max(1,rows.length)*100}%`}}/></div></div><div className="rounded-[2rem] border border-white/10 bg-black/40 p-4 sm:p-6"><div className="mb-4 flex items-center justify-between"><b className="text-lg">Objectives</b><span className="text-xs text-white/40">{done}/{rows.length}</span></div><div className="space-y-2">{rows.map(({m,p,d})=><button key={m.id} onClick={()=>complete(m)} className={`flex w-full items-center gap-3 rounded-xl border p-4 text-left ${d?'border-white/15 bg-white/10 opacity-60':'border-white/10 bg-white/[.03]'}`}><span className="flex h-9 w-9 shrink-0 items-center justify-center rounded-full bg-white/5">{d?<Check size={15}/>:<Target size={15}/>}</span><span className="min-w-0 flex-1"><b className="block">{m.title}</b><small className="mt-1 block text-xs text-white/35">{m.description}</small></span><span className="text-xs font-black text-white/45">{d?'DONE':`${p}/${m.target}`}</span></button>)}</div>{done===rows.length?<button onClick={()=>{setBossPhase(0);setScreen('boss')}} className="mt-5 flex w-full items-center justify-center gap-2 rounded-xl bg-white py-4 font-black text-black"><Swords size={17}/> ENTER BOSS CITADEL</button>:<div className="mt-4 text-xs text-white/30">Complete every objective to reveal the Boss Citadel.</div>}</div></div>;
return <WorldMap state={state} current={current} completed={state.storyChapter} open={open}/>}
function WorldMap({state,current,completed,open}:{state:ReturnType<typeof useStore>['state'];current:number;completed:number;open:(c:StoryChapter)=>void}){const[arc,setArc]=useState<'I'|'II'>('I');return <div className="relative min-h-[82vh] overflow-hidden rounded-[2rem] border border-white/10 bg-[#030506] text-white shadow-2xl"><div className="absolute inset-0 bg-[radial-gradient(ellipse_at_center,rgba(255,255,255,.07),transparent_55%)]"/><div className="absolute inset-0 opacity-20" style={{backgroundImage:'linear-gradient(rgba(255,255,255,.05) 1px,transparent 1px),linear-gradient(90deg,rgba(255,255,255,.05) 1px,transparent 1px)',backgroundSize:'48px 48px'}}/><header className="relative z-10 flex flex-wrap items-start justify-between gap-4 p-5 sm:p-7"><div><div className="flex items-center gap-2 text-[10px] font-black uppercase tracking-[.4em] text-white/35"><Map size={14}/> SYROX WORLD · CHRONICLE</div><h1 className="mt-2 text-4xl font-black sm:text-6xl">The Shattered Realm</h1><p className="mt-2 max-w-xl text-sm text-white/40">Five territories. Thirty chapters. One path through the Shadow.</p></div><div className="flex gap-2"><button onClick={()=>setArc('I')} className={`rounded-xl border px-3 py-2 text-[10px] font-black ${arc==='I'?'bg-white text-black':'border-white/10 bg-white/5 text-white/40'}`}>ARC I · ORIGIN</button><button onClick={()=>setArc('II')} className={`rounded-xl border px-3 py-2 text-[10px] font-black ${arc==='II'?'bg-white text-black':'border-white/10 bg-white/5 text-white/40'}`}>ARC II · THE BEYOND</button></div></header>{arc==='I'?<div className="relative mx-4 mb-5 overflow-hidden rounded-[1.75rem] border border-white/10 bg-[#07090b] sm:mx-7"><svg viewBox="0 0 100 100" preserveAspectRatio="none" className="absolute inset-0 h-full min-h-[620px] w-full"><path d="M0 90 Q18 72 34 78 T64 55 T100 25" fill="none" stroke="rgba(255,255,255,.06)" strokeWidth="1.2"/><path d="M0 40 Q20 25 42 35 T70 20 T100 38" fill="none" stroke="rgba(255,255,255,.045)" strokeWidth="1"/><path d={pathFor(0)} fill="none" stroke="rgba(255,255,255,.18)" strokeWidth=".8" strokeDasharray="1.8 1.2"/><path d="M5 88 L15 65 L25 72 L35 55 L47 68 L57 50 L68 60 L80 47 L92 40" fill="none" stroke="rgba(255,255,255,.035)" strokeWidth="9"/><g fill="rgba(255,255,255,.035)"><path d="M4 82 l6 -14 6 14z"/><path d="M30 60 l8 -18 8 18z"/><path d="M70 65 l7 -20 8 20z"/><path d="M82 35 l7 -18 8 18z"/></g></svg><div className="relative min-h-[620px]">{regions.map(([name,start,end])=><div key={name} className="pointer-events-none absolute rounded-full border border-white/[.04] px-3 py-1 text-[9px] font-black uppercase tracking-[.35em] text-white/[.16]" style={{left:`${nodes[Number(start)][0]}%`,top:`${Math.max(8,nodes[Number(start)][1]-13)}%`}}>{name}</div>)}{chapters.map((c,i)=>{const[x,y]=nodes[i]??[50,50];const unlocked=c.number<=current;const done=c.number<=completed;return <button key={c.id} disabled={!unlocked} onClick={()=>open(c)} style={{left:`${x}%`,top:`${y}%`}} className={`group absolute -translate-x-1/2 -translate-y-1/2 ${unlocked?'cursor-pointer':'cursor-not-allowed'}`}><span className={`relative flex h-11 w-11 items-center justify-center rounded-full border shadow-2xl transition duration-300 group-hover:scale-125 sm:h-14 sm:w-14 ${done?'border-white/70 bg-white text-black':unlocked?'border-white/40 bg-black/90 text-white':'border-white/10 bg-black/80 text-white/20'}`}>{done?<Check size={17}/>:unlocked?<Crown size={17}/>:<Lock size={15}/>}</span><span className={`mt-2 block text-[8px] font-black uppercase tracking-[.22em] ${unlocked?'text-white/60':'text-white/15'}`}>CH {String(c.number).padStart(2,'0')}</span>{unlocked&&<span className="pointer-events-none absolute bottom-full left-1/2 mb-3 hidden w-44 -translate-x-1/2 rounded-xl border border-white/10 bg-black/95 p-3 text-center shadow-xl group-hover:block"><b className="block text-[11px]">{c.title}</b><span className="mt-1 block text-[9px] text-white/35">{c.region.name}</span></span>}</button>})}</div></div>:<div className="relative mx-4 mb-5 flex min-h-[620px] items-center justify-center overflow-hidden rounded-[1.75rem] border border-white/10 bg-[radial-gradient(circle_at_50%_40%,rgba(255,255,255,.07),transparent_45%)] sm:mx-7"><div className="absolute inset-0 opacity-20"><div className="absolute left-[15%] top-[20%] h-40 w-40 rounded-full border border-white/10"/><div className="absolute right-[14%] bottom-[18%] h-56 w-56 rounded-full border border-white/10"/><Mountain className="absolute left-[20%] bottom-[20%] text-white/10" size={150}/><Mountain className="absolute right-[16%] top-[18%] text-white/10" size={190}/></div><div className="relative z-10 max-w-lg p-7 text-center"><div className="mx-auto flex h-20 w-20 items-center justify-center rounded-full border border-white/15 bg-white/5"><Sparkles size={30} className="text-white/35"/></div><div className="mt-6 text-[10px] font-black uppercase tracking-[.45em] text-white/30">ARC II · THE BEYOND</div><h2 className="mt-3 text-4xl font-black sm:text-5xl">Beyond the Shattered Realm</h2><p className="mt-4 text-sm leading-7 text-white/35">A second continent is waiting beyond the final gate. The same world-map system is ready for its territories, chapters and new bosses.</p><div className="mt-6 inline-flex items-center gap-2 rounded-xl border border-white/10 bg-white/5 px-5 py-3 text-[10px] font-black tracking-[.25em] text-white/35"><Lock size={13}/> ARC II · LOCKED</div></div></div>}<footer className="relative z-10 flex flex-wrap items-center justify-between gap-3 px-5 pb-5 text-[9px] font-black uppercase tracking-[.25em] text-white/30 sm:px-7"><span><span className="mr-2 inline-block h-2 w-2 rounded-full bg-white"/> {completed}/30 territories conquered</span><span>Current Chapter {current}/30</span><span className="flex items-center gap-2"><Skull size={12}/> Each territory ends at a Boss Citadel</span></footer></div>}
